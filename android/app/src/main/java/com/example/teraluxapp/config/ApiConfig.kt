package com.example.teraluxapp.config

object ApiConfig {
    // 10.0.2.2 is the special alias to your host loopback interface (localhost)
    // from the android emulator
    val BASE_URLS = listOf(
        "https://teralux.iotunnel.my.id/",
        "https://teralux.farismunir.my.id/"
    )
}
